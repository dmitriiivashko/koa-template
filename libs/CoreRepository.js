export default class CoreRepository {
  /**
   * Creates an instance of ClientsRepository.
   *
   * @param {any} { db }
   * @memberof CoreRepository
   */
  constructor({ db }) {
    this.db = db;
  }
}
